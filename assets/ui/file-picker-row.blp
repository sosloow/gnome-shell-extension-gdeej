using Gtk 4.0;
using Adw 1;

template $DeejFilePickerRow: Adw.EntryRow {
    title: _('Select device');

    Gtk.Button file-button {
        icon-name: 'document-open-symbolic';
        margin-top: 10;
        margin-bottom: 10;
    }
}
